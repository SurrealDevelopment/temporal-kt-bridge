//! Client and RPC support.
//!
//! Entry points land here as the rewrite proceeds; the handle-table variant exists already so the
//! table's shape is fixed up front rather than churned per module.

/// Placeholder while this module is built out.
pub struct ClientEntry {
    pub _runtime: u64,
}
